CREATE TABLE alunos (
id SERIAL PRIMARY KEY,
nome VARCHAR(100),
email VARCHAR(100)
);

CREATE TABLE cursos (
id SERIAL PRIMARY KEY,
nome VARCHAR(100),
carga_horaria INTEGER
);
INSERT INTO alunos (nome, email)
VALUES ('João', 'joao@gmail.com');

SELECT * FROM alunos;
UPDATE alunos SET nome='Jãozin' WHERE id=1;